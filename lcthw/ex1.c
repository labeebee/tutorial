#include <stdio.h>

/* This is a comment */
int main(int argc, char *argv[])
{
  int distance = 100;
  // This is also a comment

  printf("You are %d miles away. \n", distance);
  printf("You couild go to %s from here. \n", "Washington");
  printf("You could take a bus or a plane.\n");
  printf("But it would be funny to take a plabe that leaves after %d hrs. \n", 24);
  printf("Remember, the distance is just %d miles. Good luck planning.  \n", distance);
  
  return 0;
}
